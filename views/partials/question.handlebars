<div class="question">
  {{#each this}}
  <section class="questions">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{question_text}}
    </div>
  </section>
  {{/each}
